These tools can be redistributed to end users if retrieval of their 
system ID or USB ID via the protection API is not possible or feasible. 
They need to be protected using the same licensing settings as the 
application in question, e.g. by adding them to the project file.